# 貸出端末 管理画面

オンラインデイサービス長老大学の貸出専用端末(呼び出し型)を事務局が操作する画面。
正本は `sawamoto-llc/chourou-callapp` の `admin/index.html`。変更はそちらで行い、ここへコピーする。

- ログインが必要(事務局スタッフのみ)。HTML に秘密は含まれない(公開用キーのみ)。
- 検索エンジンには載せない(robots.txt / noindex)。
